/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run nf-core/oncoseq -profile test,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {
    resourceLimits = [
        cpus: 4,
        memory: '15.GB',
        time: '1.h'
    ]
    maxForks = 1
}

params {
    config_profile_name        = 'Test profile'
    config_profile_description = 'Minimal test dataset to check pipeline function'
    outdir = 'test_out'
    // Input data
    input  = params.pipelines_testdata_base_path + 'basecalling/samplesheets/samplesheet.csv'
    model = 'fast'
    device = null
    // TODO mpgi: make a better dataset for demultiplexing, possibly cfnda
    //demux = 'SQK-NBD114-24'
    //demux_samplesheet = params.pipelines_testdata_base_path + 'samplesheets/demux_samplesheet.csv'
   // ubam_samplesheet = params.pipelines_testdata_base_path + 'samplesheets/ubam_samplesheet.csv'
    publish_dir_mode = 'copy'
    minqs = 8
    bed = params.pipelines_testdata_base_path + 'adaptive/2025-02-05-CHUSJ-Panel-20kb-LP.bed'
    max_cpus = 2
    max_memory = '15.GB'
    max_time = '1.h'
}

apptainer {
    enabled=true
    autoMounts=true
    runOptions = '--writable-tmpfs'
    cacheDir = "/tmp/apptainer/cache"
}
