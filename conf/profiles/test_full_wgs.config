/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running full-size tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a full size pipeline test.

    Use as follows:
        nextflow run nf-core/oncoseq -profile test_full,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {
    errorStrategy = { task.attempt < 2 ? 'retry' : 'ignore' }
    maxRetries = 1
    resourceLimits = [
        cpus: 8,
        memory: '30.GB',
        time: '3.h'
    ]
}

params {
    config_profile_name        = 'Full test profile for WGS'
    config_profile_description = 'Full test dataset to check pipeline function'
    outdir = 'test_out'
    // Input data for full size test
    input = params.pipelines_testdata_base_path + 'realistic_data/fastq_samplesheet.csv'
    clairsto_model             = 'ont_r10_dorado_sup_5khz_ssrs'
    basecall_model             = 'hac'
    maxForks = 1
    publish_dir_mode = 'copy'
    wgs = true
    ref_cache = "${projectDir}/test_data/realistic_data/"
    skip_basecalling = true
    delly_bin_size = 10000
}

apptainer {
    enabled=true
    autoMounts=true
    runOptions = '--writable-tmpfs'
    cacheDir = "/tmp/apptainer/cache"
}
