nextflow_process {

    name "Test Process ONTIME_RANGE_FILTER"
    script "modules/local/ontime/main.nf"
    process "ONTIME_RANGE_FILTER"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = tuple [id: "test_sample"], file("${projectDir}/test_data/alignments/sample1_mito_align.bam")
                input[1] = tuple "0", "2"
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
