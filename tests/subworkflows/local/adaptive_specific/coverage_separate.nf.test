nextflow_workflow {

    name "Test Workflow COVERAGE_SEPARATE"
    script "subworkflows/local/adaptive_specific/coverage_separate.nf"
    workflow "COVERAGE_SEPARATE"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            workflow {
                """
                // define inputs of the workflow here
                input[0] = tuple(
                    [id: "test_sample"],
                    file("${projectDir}/test_data/alignments/sample1_mito_align.bam"),
                    file("${projectDir}/test_data/alignments/sample1_mito_align.bam.bai")
                )
                input[1] = tuple(
                    [id: "test_sample"],
                    file("${projectDir}/test_data/adaptive/2025-02-05-CHUSJ-Panel-20kb-LP.bed"),
                    100,
                    false
                )
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

}
